const User = require('./User');

// As new models are added, import them here and define associations below.
// Example:
// const Activity = require('./Activity');
// User.hasMany(Activity);

module.exports = { User };
